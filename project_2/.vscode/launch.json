{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "linker debug",
            "program": "${workspaceFolder}/linker",
            "cwd": "${workspaceFolder}",
        	"preRunCommands": ["breakpoint set --name main"],
            "args": ["count5_0.obj.correct", "count5_1.obj.correct", "count5_linked.mc"],
		// ^ adds a breakpoint at the beginning of main
        }
    ]
}
